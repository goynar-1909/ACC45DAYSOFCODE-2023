#include<stdio.h>


int main() {
	// your code goes here
	int t,x,y,i;
	scanf("%d",&t);
	while(t--)
	{ scanf("%d%d",&x,&y);
      if((y/x)*x<y)
	    { printf("%d",y/x);}
	    else 
	    { printf("%d",y/x-1);}
	    
	}
	return 0;
}
